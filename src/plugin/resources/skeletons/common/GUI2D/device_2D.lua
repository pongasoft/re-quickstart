format_version = "2.0"

--------------------------------------------------------------------------
-- front
--------------------------------------------------------------------------
front = {
    Panel_front_bg = {
        { path = "[-device2D-front_bg-]" }
    }
}

[-device2D-front-]

--------------------------------------------------------------------------
-- folded_front
--------------------------------------------------------------------------
folded_front = {
    Panel_folded_front_bg = {
        { path = "[-device2D-folded_front_bg-]" }
    }
}

[-device2D-folded_front-]

--------------------------------------------------------------------------
-- back
--------------------------------------------------------------------------
back = {
    Panel_back_bg = {
        { path = "[-device2D-back_bg-]" }
    }
}

[-device2D-back-]

--------------------------------------------------------------------------
-- folded_back
--------------------------------------------------------------------------
folded_back = {
    Panel_folded_back_bg = {
        { path = "[-device2D-folded_back_bg-]" }
    },
    CableOrigin = {
        offset = { 1885, 75 }
    }
}

[-device2D-folded_back-]
