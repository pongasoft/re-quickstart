format_version = "2.0"

panel_type = "note_player"

--------------------------------------------------------------------------
-- front
--------------------------------------------------------------------------
front = {
    Panel_front_bg = {
        { path = "[-device2D-front_bg-]" }
    }
}

[-device2D-front-]

--------------------------------------------------------------------------
-- back
--------------------------------------------------------------------------
back = {
    Panel_back_bg = {
        { path = "[-device2D-back_bg-]" }
    }
}

[-device2D-back-]
